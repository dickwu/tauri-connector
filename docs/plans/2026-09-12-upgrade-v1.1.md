# Upgrade v1.1 execution plan

Authoritative request: implement U0–U7 from the supplied upgrade v1.1 specification, with full active element picking and 144 individually reported acceptance requirements. Baseline HEAD: `fb43c4ac11f04813b2a5038f52223fe9bcb246ed`; worktree initially clean.

1. Establish reproducible baseline, locked browser fixtures and CI jobs. Inventory every UP-T and UP-PK requirement before claiming coverage.
2. Install trusted page modules once with app/window/document/runtime identity. Preserve workflow dispatch uncertainty and immutable specifications.
3. Enforce strict discovery and identity-bound routing. Share bounded semantic resolution across snapshots, locators, waits and picker candidates.
4. Implement actual native WebView screenshot adapters with geometry validation, masking and protected artifacts. Record platform builds separately from native runs.
5. Implement application-owned picker lifecycle, atomic request deduplication, authorization generation, resource leases, early input isolation, visible UI, verified candidates, screenshot completion and bounded teardown. Four adapters call this service.
6. Add independent bounded IPC capture sessions with safe previews and preserved invoke semantics.
7. Run regressions, native fixture scenarios, feature builds, formatting, Clippy and independent review. Update the 144-row status report from actual evidence, retaining all unverified/blocked distinctions.

Parallel ownership: runtime/identity, semantics, screenshots, capture, adapters/routing, and CI/documentation are independent lanes; the leader owns PickerService and integration. Each lane adds failing regressions before behavior changes and records commands in `docs/upgrade-evidence/`.

No version bump, publication, tag, push, remote PR, deployment, business data mutation, workflow spec rewriting, replay of unknown writes, or quarantine relaxation is authorized by this implementation request.
